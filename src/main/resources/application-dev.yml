server:
  servlet:
    encoding:
      charset: UTF-8
      force: true

  port: 8080

# 로그 레벨의 개념,
# ERROR > WARN > INFO > DEBUG > TRACE
logging:
  level:
    root: INFO #모든 라이브러리는 info 레벨 이상만 출력
    com.tenco: DEBUG # 프로젝트는 DEBUG 이상 모두 출력